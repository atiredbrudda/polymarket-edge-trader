---
phase: 18-end-to-end-validation
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/gamma/position_resolver.py
  - tests/test_position_resolver.py
  - src/cli/commands.py
autonomous: true
requirements:
  - E2E-01
  - E2E-02

must_haves:
  truths:
    - "resolve_positions() sets Position.resolved=True, outcome='win'/'loss'/'flat'/'void', and pnl for every position whose market has a non-NULL outcome"
    - "LONG on YES market → outcome='win', pnl > 0; LONG on NO market → outcome='loss', pnl < 0"
    - "SHORT on YES market → outcome='loss'; SHORT on NO market → outcome='win'"
    - "FLAT positions get outcome='flat', pnl=0, resolved=True"
    - "Re-running resolve_positions() on already-resolved positions changes nothing (idempotent)"
    - "polymarket resolve-positions CLI command calls resolve_positions, commits, and prints resolved count"
  artifacts:
    - path: "src/gamma/position_resolver.py"
      provides: "resolve_positions(session) function"
      exports: ["resolve_positions"]
    - path: "tests/test_position_resolver.py"
      provides: "TDD tests covering LONG/SHORT/FLAT/VOID/idempotency cases"
      min_lines: 80
    - path: "src/cli/commands.py"
      provides: "resolve-positions CLI command"
      contains: "resolve-positions"
  key_links:
    - from: "src/cli/commands.py"
      to: "src/gamma/position_resolver.py"
      via: "import resolve_positions"
      pattern: "from src\\.gamma\\.position_resolver import resolve_positions"
    - from: "src/gamma/position_resolver.py"
      to: "Position.resolved"
      via: "ORM update in loop"
      pattern: "position\\.resolved = True"
    - from: "src/gamma/position_resolver.py"
      to: "Market.outcome"
      via: "join query filtering outcome IS NOT NULL"
      pattern: "Market\\.outcome\\.is_not\\(None\\)"
---

<objective>
Implement resolve_positions() using TDD — the missing bridge between Market.outcome (set by Phase 16) and Position.resolved/outcome/pnl (required by scoring pipeline). Wire to a new resolve-positions CLI command following the resolve-outcomes pattern.

Purpose: The scoring pipeline filters `p.resolved == True`. Currently ZERO positions have resolved=True because no code has ever set it. This plan adds the only missing piece in the data pipeline before E2E scoring can work.
Output: src/gamma/position_resolver.py (function + tests), resolve-positions CLI command in commands.py
</objective>

<execution_context>
@/Users/macbookair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/macbookair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-end-to-end-validation/18-RESEARCH.md

# Prior phase patterns used here
@.planning/phases/16-market-outcome-resolution/16-02-SUMMARY.md

# Key source files
@src/gamma/resolution.py
@src/db/models.py
@src/cli/commands.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: TDD — resolve_positions() in src/gamma/position_resolver.py</name>
  <files>src/gamma/position_resolver.py, tests/test_position_resolver.py</files>

  <feature>
    <name>resolve_positions</name>
    <files>src/gamma/position_resolver.py, tests/test_position_resolver.py</files>
    <behavior>
      Function signature: resolve_positions(session: Session) -> dict[str, int]
      Returns: {"resolved": N, "skipped_no_outcome": M, "skipped_already_resolved": K}

      Key behaviors to test (write tests BEFORE implementation):

      Case 1 — LONG on YES market:
        - Position(direction="LONG", size=100, avg_entry_price=0.6, resolved=False)
        - Market(outcome="YES")
        - Expected: position.resolved=True, position.outcome="win"
        - PnL: 100 * (1.0 - 0.6) = 40.0 (positive)

      Case 2 — LONG on NO market:
        - Position(direction="LONG", size=100, avg_entry_price=0.6, resolved=False)
        - Market(outcome="NO")
        - Expected: position.resolved=True, position.outcome="loss"
        - PnL: 100 * (0.0 - 0.6) = -60.0 (negative)

      Case 3 — SHORT on NO market:
        - Position(direction="SHORT", size=100, avg_entry_price=0.4, resolved=False)
        - Market(outcome="NO")
        - Expected: position.resolved=True, position.outcome="win"
        - PnL: 100 * (0.4 - 0.0) = 40.0 (positive)

      Case 4 — SHORT on YES market:
        - Position(direction="SHORT", size=100, avg_entry_price=0.4, resolved=False)
        - Market(outcome="YES")
        - Expected: position.resolved=True, position.outcome="loss"
        - PnL: 100 * (0.4 - 1.0) = -60.0 (negative, use abs(size)*(entry-resolution))

      Case 5 — FLAT position:
        - Position(direction="FLAT", size=0, resolved=False)
        - Market(outcome="YES")
        - Expected: position.resolved=True, position.outcome="flat", position.pnl=0
        - (Size is 0 or direction is FLAT — fully closed before resolution)

      Case 6 — Market outcome is NULL:
        - Position(resolved=False), Market(outcome=None)
        - Expected: position NOT resolved, counts as skipped_no_outcome

      Case 7 — Idempotency:
        - Position already has resolved=True
        - Expected: position skipped (not re-processed), counts as skipped_already_resolved

      Case 8 — avg_entry_price is NULL:
        - Position(direction="LONG", size=100, avg_entry_price=None)
        - Market(outcome="YES")
        - Expected: position.resolved=True, position.pnl=Decimal("0"), outcome="flat" (cannot compute meaningful PnL without entry price)

      Case 9 — Market outcome="VOID" or other unexpected value:
        - Expected: position.resolved=True, position.outcome="void", position.pnl=Decimal("0")
    </behavior>
    <implementation>
      Create src/gamma/position_resolver.py following the pattern of src/gamma/resolution.py:

      1. Imports: Decimal from decimal, logger from loguru, Session from sqlalchemy.orm, Position and Market from src.db.models

      2. Module docstring: "Position resolution from resolved market outcomes."

      3. resolve_positions(session: Session) -> dict[str, int]:
         - Docstring: "Populate Position.resolved, outcome, pnl for positions on resolved markets. Caller must commit."
         - Query: session.query(Position).join(Market, Position.market_id == Market.condition_id).filter(Market.outcome.is_not(None)).filter(Position.resolved == False).all()
         - For each position:
           * market = session.query(Market).filter_by(condition_id=position.market_id).one_or_none()
           * If market is None or market.outcome is None: skipped_no_outcome += 1; continue
           * market_outcome = market.outcome  # "YES" or "NO"
           * VOID handling: if market_outcome not in ("YES", "NO"): position.resolved=True; position.outcome="void"; position.pnl=Decimal("0"); resolved+=1; continue
           * FLAT handling: if position.direction == "FLAT" or position.size == Decimal("0"): position.resolved=True; position.outcome="flat"; position.pnl=Decimal("0"); resolved+=1; continue
           * NULL price handling: if position.avg_entry_price is None: position.resolved=True; position.outcome="flat"; position.pnl=Decimal("0"); resolved+=1; continue
           * resolution_price: Decimal("1.0") if market_outcome == "YES" else Decimal("0.0")
           * PnL for LONG: pnl = position.size * (resolution_price - position.avg_entry_price)
           * PnL for SHORT: pnl = position.size * (position.avg_entry_price - resolution_price)
           * outcome = "win" if pnl > Decimal("0") else "loss" if pnl < Decimal("0") else "flat"
           * position.resolved = True; position.outcome = outcome; position.pnl = pnl; resolved += 1
         - logger.info at end with counts
         - Return {"resolved": resolved, "skipped_no_outcome": skipped_no_outcome, "skipped_already_resolved": 0}

      CRITICAL: Do NOT call session.commit() inside resolve_positions(). The CLI handles commit.

      Tests use SQLite in-memory DB following the pattern in tests/test_gamma_resolution.py (TestResolveMarketOutcomes.test_idempotent_re_run). Create Position + Market ORM objects, call resolve_positions(session), then assert position fields.

      Join key: Position.market_id == Market.condition_id (confirmed from models.py).
    </implementation>
  </feature>

  <action>
    Follow the RED-GREEN-REFACTOR TDD cycle:

    RED: Create tests/test_position_resolver.py with all 9 test cases described in the feature behavior block. Run pytest — tests must FAIL (ImportError or AssertionError). Commit: test(18-01): add failing tests for resolve_positions

    GREEN: Create src/gamma/position_resolver.py implementing resolve_positions() as described in the feature implementation block. Run pytest — all tests must PASS. Commit: feat(18-01): implement resolve_positions

    REFACTOR (if needed): Clean up without changing behavior. Run tests. Commit: refactor(18-01): clean up resolve_positions
  </action>

  <verify>
    RED phase: cd /Users/macbookair/Documents/project/test/rerun7/GSD_Polymarket && source venv/bin/activate && python -m pytest tests/test_position_resolver.py -v 2>&1 | tail -20
    (must show FAILED or ImportError — no test_position_resolver.py exists yet)

    GREEN phase: python -m pytest tests/test_position_resolver.py -v 2>&1 | tail -20
    (must show all tests PASSED)

    Full suite check: python -m pytest tests/ -x -q 2>&1 | tail -10
    (no regressions)
  </verify>
  <done>
    All test cases (Cases 1-9) pass. resolve_positions() sets correct outcome/pnl for LONG/SHORT/FLAT/VOID/NULL-price positions. Full test suite has no regressions. src/gamma/position_resolver.py exists and exports resolve_positions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire resolve-positions CLI command</name>
  <files>src/cli/commands.py</files>
  <action>
    Add the resolve-positions CLI command to src/cli/commands.py following the exact pattern of the resolve-outcomes command (lines 2135-2198).

    1. Add import at top of file alongside the other gamma imports:
       from src.gamma.position_resolver import resolve_positions

    2. Add CLI command after the resolve-outcomes command block (after line ~2198):

    @cli.command("resolve-positions")
    @click.option("--verbose", "-v", is_flag=True, help="Enable debug logging")
    def resolve_positions_cmd(verbose):
        """Populate Position.resolved, outcome, and pnl from resolved market outcomes.

        Reads positions table and joins with markets where markets.outcome is
        non-NULL (populated by resolve-outcomes). For each such position,
        computes win/loss/flat based on position direction and market result.

        Resolution logic:
        - LONG + YES market -> win (pnl = size * (1.0 - avg_entry_price))
        - LONG + NO market -> loss (pnl = size * (0.0 - avg_entry_price))
        - SHORT + NO market -> win
        - SHORT + YES market -> loss
        - FLAT positions -> flat, pnl=0
        - Already-resolved positions are skipped (idempotent)

        Safe to re-run. Run after 'polymarket resolve-outcomes'.

        Examples:
            polymarket resolve-positions
            polymarket resolve-positions --verbose
        """
        logger.info("RESOLVE-POSITIONS command started")

        if verbose:
            logger.remove()
            logger.add(sys.stderr, level="DEBUG")

        console = Console()

        try:
            settings = get_settings()
            session_factory, _, _, _, _ = _get_dependencies(settings)

            console.print("[bold]Resolving positions from market outcomes...[/bold]")

            with get_session(session_factory) as session:
                counts = resolve_positions(session)
                session.commit()

            resolved = counts["resolved"]
            skipped_no_outcome = counts["skipped_no_outcome"]

            console.print(
                f"[green]Done.[/green] {resolved} positions resolved."
            )
            console.print(
                f"  Positions skipped (no market outcome): [yellow]{skipped_no_outcome}[/yellow]"
            )
            logger.info(
                f"RESOLVE-POSITIONS completed: {resolved} resolved, "
                f"{skipped_no_outcome} skipped"
            )

        except Exception as e:
            logger.error(f"resolve-positions failed: {e}")
            console.print(f"[red]Error: {e}[/red]")
            raise SystemExit(1)

    Note: The function is named resolve_positions_cmd to avoid shadowing the imported resolve_positions function.
  </action>
  <verify>
    source /Users/macbookair/Documents/project/test/rerun7/GSD_Polymarket/venv/bin/activate && polymarket resolve-positions --help
    (must show correct help text with the command name and --verbose option)

    python -m pytest tests/ -x -q 2>&1 | tail -10
    (no regressions)
  </verify>
  <done>
    polymarket resolve-positions --help shows correct docstring. No test regressions. Import of resolve_positions at top of commands.py is present.
  </done>
</task>

</tasks>

<verification>
1. tests/test_position_resolver.py exists and all tests pass
2. src/gamma/position_resolver.py exists and exports resolve_positions
3. polymarket resolve-positions --help works
4. python -m pytest tests/ -x -q passes with no regressions
</verification>

<success_criteria>
- TDD cycle complete: RED->GREEN confirmed for all 9 test cases
- LONG/SHORT/FLAT/VOID/NULL-price cases all handled correctly
- resolve-positions CLI command works end-to-end (help text, no crash on run)
- No regressions in full test suite
</success_criteria>

<output>
After completion, create .planning/phases/18-end-to-end-validation/18-01-SUMMARY.md
</output>
