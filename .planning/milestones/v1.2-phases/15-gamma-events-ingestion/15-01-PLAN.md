---
phase: 15-gamma-events-ingestion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/models.py
  - src/api/gamma_client.py
autonomous: true
requirements:
  - GAMMA-01
  - GAMMA-02

must_haves:
  truths:
    - "GammaEvent table exists in the database after app startup"
    - "GammaMarketClient can download all ~8,500 closed eSports events in one call"
    - "Events returned include outcomePrices, clobTokenIds, and tags fields"
  artifacts:
    - path: "src/db/models.py"
      provides: "GammaEvent ORM model with all required fields"
      contains: "class GammaEvent"
    - path: "src/api/gamma_client.py"
      provides: "get_closed_esports_events() bulk download method"
      contains: "def get_closed_esports_events"
  key_links:
    - from: "src/db/models.py"
      to: "SQLite gamma_events table"
      via: "Base.metadata.create_all() auto-migration on startup"
      pattern: "class GammaEvent.*Base"
    - from: "src/api/gamma_client.py"
      to: "gamma-api.polymarket.com/events"
      via: "httpx.get with tag_id=64, active=false, limit=200"
      pattern: "get_closed_esports_events"
---

<objective>
Add the GammaEvent database model and bulk download method that Phase 15 builds on.

Purpose: Establishes the two foundational pieces — the storage schema and the API retrieval method — before the CLI and persistence layer are wired together in Plan 02.
Output: GammaEvent ORM model in models.py + get_closed_esports_events() method in GammaMarketClient.
</objective>

<execution_context>
@/Users/macbookair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/macbookair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/models.py
@src/api/gamma_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GammaEvent ORM model to src/db/models.py</name>
  <files>src/db/models.py</files>
  <action>
Add a new `GammaEvent` class to the bottom of `src/db/models.py` following the existing SQLAlchemy 2.0 `Mapped[]` style.

Model fields (all based on the Gamma API `/events` response shape):
- `event_id: Mapped[str]` — primary key, the `id` field from the API (e.g., "252234")
- `title: Mapped[str | None]` — `title` field, max 500 chars
- `slug: Mapped[str | None]` — `slug` field, max 200 chars (URL-safe identifier)
- `outcome_prices: Mapped[str | None]` — JSON string from `outcomePrices` field (a list like `["0.99", "0.01"]`)
- `clob_token_ids: Mapped[str | None]` — JSON string from `clobTokenIds` (a list of token ID strings); this field appears on markets[] nested inside the event — store as aggregated JSON list of ALL token IDs across all markets in this event
- `tags: Mapped[str | None]` — JSON string from `tags[]` field (array of tag objects with `slug`, `id`, `label` fields)
- `start_date: Mapped[datetime | None]` — parsed from `startDate` ISO string
- `end_date: Mapped[datetime | None]` — parsed from `endDate` ISO string
- `created_at: Mapped[datetime]` — default=datetime.utcnow
- `updated_at: Mapped[datetime]` — default=datetime.utcnow, onupdate=datetime.utcnow

Table name: `gamma_events`

Add index on `end_date` for date-range queries: `Index("ix_gamma_event_end_date", "end_date")`.

Follow the exact style of the existing `Market` model — use `mapped_column()`, `String(N)` for text, standard `__table_args__` tuple pattern.

The `Base.metadata.create_all()` call in `src/db/session.py` handles table creation automatically — do NOT add a migration script.

Do NOT change any existing model.
  </action>
  <verify>
Run from the project root (venv activated):
```bash
python -c "from src.db.models import GammaEvent; print('GammaEvent OK:', GammaEvent.__tablename__)"
```
Expected output: `GammaEvent OK: gamma_events`
  </verify>
  <done>
`GammaEvent` class exists in `src/db/models.py` with `__tablename__ = "gamma_events"`, all fields typed with `Mapped[]`, and `ix_gamma_event_end_date` index defined. Import resolves without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add get_closed_esports_events() to GammaMarketClient</name>
  <files>src/api/gamma_client.py</files>
  <action>
Add a new method `get_closed_esports_events()` to the `GammaMarketClient` class in `src/api/gamma_client.py`.

This method downloads ALL closed eSports events in one call (active=false, tag_id=64). It replaces manually calling `get_events()` with the right parameters — the method encapsulates the correct defaults for Phase 15 ingestion.

Method signature:
```python
def get_closed_esports_events(
    self,
    page_size: int = 200,
) -> list[dict[str, Any]]:
```

Implementation:
1. Use offset-based pagination (same pattern as existing `get_events()`): start at offset=0, increment by `page_size`, stop when response is empty or shorter than `page_size`.
2. Request params: `active=false`, `tag_id=64`, `limit=page_size`, `offset=offset`, `order=endDate`, `ascending=true`.
3. Use `httpx.get(self.BASE_URL + "/events", params=params, timeout=60.0)` — longer timeout than other methods since this downloads ~8,500 events (~10MB total).
4. Respect `self.rate_limiter` if set (same pattern as existing methods: `if self.rate_limiter is not None: self.rate_limiter.acquire()`).
5. Log progress: `logger.info(f"Fetched {len(events)} events (total so far: {len(all_events)})")` after each page.
6. On success, log: `logger.info(f"Downloaded {len(all_events)} closed eSports events from Gamma API")`.
7. Return the full list of event dicts (raw, unparsed — caller handles field extraction).

Add docstring:
```
Fetch ALL closed eSports events (tag_id=64) from Gamma API.

Used for Phase 15 bulk ingestion. Downloads ~8,500 events (~30s).
No active/date filtering — fetches the complete historical set.

Args:
    page_size: Events per API request (default 200, max 500 per Gamma API)

Returns:
    List of event dicts with fields: id, title, slug, outcomePrices,
    markets[].clobTokenIds, tags[], startDate, endDate
```

Do NOT modify any existing methods. Add the new method after `get_events()` and before `get_public_profile()`.
  </action>
  <verify>
Run from the project root (venv activated):
```bash
python -c "
from src.api.gamma_client import GammaMarketClient
import inspect
sig = inspect.signature(GammaMarketClient.get_closed_esports_events)
print('Method OK, params:', list(sig.parameters.keys()))
"
```
Expected: `Method OK, params: ['self', 'page_size']`

Then do a live smoke test to confirm API reachability:
```bash
python -c "
from src.api.gamma_client import GammaMarketClient
client = GammaMarketClient()
# Just fetch first page to verify connectivity
import httpx
resp = httpx.get('https://gamma-api.polymarket.com/events', params={'active': 'false', 'tag_id': 64, 'limit': 5}, timeout=30)
print('API status:', resp.status_code, '| events in page:', len(resp.json()))
"
```
Expected: `API status: 200 | events in page: 5`
  </verify>
  <done>
`GammaMarketClient.get_closed_esports_events()` method exists with correct signature. API reachability confirmed (status 200). Method uses offset-based pagination, respects rate_limiter, logs progress per page, and returns list of raw event dicts.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `python -c "from src.db.models import GammaEvent; print(GammaEvent.__tablename__)"` prints `gamma_events`
2. `python -c "from src.api.gamma_client import GammaMarketClient; c = GammaMarketClient(); print(hasattr(c, 'get_closed_esports_events'))"` prints `True`
3. No existing tests broken: `python -m pytest tests/ -x -q 2>&1 | tail -5`
</verification>

<success_criteria>
- GammaEvent model importable with all 9 fields typed correctly
- get_closed_esports_events() method present and callable on GammaMarketClient
- Gamma API returns 200 for closed eSports events (connectivity confirmed)
- All existing tests pass (0 regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/15-gamma-events-ingestion/15-01-SUMMARY.md` with:
- What was implemented (model fields, method signature)
- Any API shape observations (what outcomePrices / tags look like in practice)
- Verification results
</output>
