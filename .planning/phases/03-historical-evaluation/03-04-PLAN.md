---
phase: 03-historical-evaluation
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/db/models.py
  - src/pipeline/queries.py
  - tests/test_evaluation_queries.py
autonomous: true

must_haves:
  truths:
    - "PerformanceSnapshot model stores realized and unrealized PnL, win rate, volume, profile type, and consistency data per trader per timeframe"
    - "TraderProfile model stores profile classification (selective/active) and unique market count"
    - "Time-windowed queries efficiently retrieve positions and trades within rolling windows"
    - "Resolution grace period of 4 hours (2x UMA 2-hour challenge period per research) filters recently-resolved markets from metrics"
    - "Composite indexes on (trader_address, timestamp) and (trader_address, resolved) enable fast queries"
  artifacts:
    - path: "src/db/models.py"
      provides: "PerformanceSnapshot and TraderProfile ORM models"
      contains: "class PerformanceSnapshot"
    - path: "src/pipeline/queries.py"
      provides: "Time-windowed query functions for evaluation"
      exports: ["get_positions_by_timeframe", "get_resolved_positions", "get_trader_unique_markets", "get_trader_outcomes_chronological"]
    - path: "tests/test_evaluation_queries.py"
      provides: "Tests for new query functions"
  key_links:
    - from: "src/pipeline/queries.py"
      to: "src/db/models.py"
      via: "SQLAlchemy queries on Position and PerformanceSnapshot models"
      pattern: "select.*Position|PerformanceSnapshot"
---

<objective>
Add database models and query functions for historical evaluation storage and retrieval.

Purpose: The evaluation module (Plans 01-03) produces metrics, timeframe snapshots, profiles, and consistency data. This plan provides the persistence layer: DB models to store evaluation results and optimized queries to fetch time-windowed data for computation.

Output: Extended `src/db/models.py` with PerformanceSnapshot and TraderProfile models, and extended `src/pipeline/queries.py` with evaluation-specific queries.
</objective>

<execution_context>
@/Users/macbookair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/macbookair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/models.py  — Existing models (Market, Trader, Trade, Position, etc.)
@src/pipeline/queries.py  — Existing query functions
@src/db/session.py  — Session management pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PerformanceSnapshot and TraderProfile DB models</name>
  <files>src/db/models.py</files>
  <action>
    Add two new ORM models to the existing models.py file:

    1. TraderProfile model:
       - id: Mapped[int] primary key
       - trader_address: Mapped[str] String(42), unique, indexed
       - profile_type: Mapped[str] String(10) ("selective" or "active")
       - unique_markets: Mapped[int] (count of unique markets entered)
       - total_trades: Mapped[int] (total trade count)
       - threshold_used: Mapped[int] (threshold for classification)
       - computed_at: Mapped[datetime] default utcnow
       - Index on trader_address (unique)

    2. PerformanceSnapshot model:
       - id: Mapped[int] primary key
       - trader_address: Mapped[str] String(42), not null
       - timeframe: Mapped[str] String(10) ("7d", "30d", "90d", "all")
       - realized_pnl: Mapped[Decimal] Numeric(20,6) default 0
       - unrealized_pnl: Mapped[Decimal] Numeric(20,6) default 0
       - total_pnl: Mapped[Decimal] Numeric(20,6) default 0
       - wins: Mapped[int] default 0
       - losses: Mapped[int] default 0
       - total_resolved: Mapped[int] default 0
       - win_rate: Mapped[Decimal | None] Numeric(10,6) nullable
       - total_volume: Mapped[Decimal] Numeric(20,6) default 0
       - resolved_markets: Mapped[int] default 0
       - unresolved_markets: Mapped[int] default 0
       - is_low_confidence: Mapped[bool] default False (flagged when < 5 resolved per user decision)
       - consistency_score: Mapped[Decimal | None] Numeric(10,6) nullable
       - consistency_signal: Mapped[str | None] String(20) nullable ("stable", "streaky", "insufficient_data")
       - profile_type: Mapped[str | None] String(10) nullable
       - computed_at: Mapped[datetime] default utcnow
       - Composite unique index on (trader_address, timeframe) for upsert
       - Index on (trader_address) for trader-level queries
       - Index on (timeframe) for cross-trader comparisons

    Follow existing patterns: Numeric(20,6) for volumes/pnl, Numeric(10,6) for rates/prices, datetime defaults, composite indexes.
  </action>
  <verify>
    ```bash
    cd /Users/macbookair/Documents/project/test/rerun7/GSD_Polymarket
    source .venv/bin/activate
    python -c "from src.db.models import PerformanceSnapshot, TraderProfile; print('Models imported successfully')"
    ```
  </verify>
  <done>PerformanceSnapshot and TraderProfile models importable with all specified fields and indexes.</done>
</task>

<task type="auto">
  <name>Task 2: Add time-windowed evaluation queries</name>
  <files>src/pipeline/queries.py, tests/test_evaluation_queries.py</files>
  <action>
    Add the following query functions to the existing queries.py file:

    1. get_positions_by_timeframe(session, trader_address, window_key, now=None) -> list[Position]:
       - Import get_timeframe_bounds from src.evaluation.timeframes
       - Get (start, end) bounds for the window
       - Query Position where trader_address matches
       - If start is not None: filter last_trade_timestamp >= start
       - Filter last_trade_timestamp <= end
       - Order by last_trade_timestamp DESC
       - Return list of Position ORM objects

    2. get_resolved_positions(session, trader_address, grace_period_hours=4) -> list[Position]:
       - Query Position where trader_address matches AND resolved == True
       - Exclude positions where the market was resolved within the last grace_period_hours
         (implementation: join Market, check market.updated_at < now - grace_period timedelta)
       - Per research: 4-hour grace period (2x UMA 2-hour challenge period)
       - Order by last_trade_timestamp DESC

    3. get_trader_unique_markets(session, trader_address) -> int:
       - Query count of distinct market_id from Position where trader_address matches
       - Return int count

    4. get_trader_outcomes_chronological(session, trader_address) -> list[str]:
       - Query Position where trader_address matches AND resolved == True AND outcome is not None
       - Filter out outcome == "void" and outcome == "flat"
       - Order by last_trade_timestamp ASC (chronological for streak analysis)
       - Return list of outcome strings ["win", "loss", "win", ...]

    Write tests in tests/test_evaluation_queries.py:
    - Use in-memory SQLite (following existing test patterns in tests/test_queries.py)
    - Test get_positions_by_timeframe with 7d window filtering
    - Test get_resolved_positions excludes grace period
    - Test get_trader_unique_markets counts correctly
    - Test get_trader_outcomes_chronological returns correct order, excludes void/flat

    Import new Position model fields for resolution filtering. The Position model already has `resolved`, `outcome`, and timestamp fields.
  </action>
  <verify>
    ```bash
    cd /Users/macbookair/Documents/project/test/rerun7/GSD_Polymarket
    source .venv/bin/activate
    python -m pytest tests/test_evaluation_queries.py -v
    ```
  </verify>
  <done>All 4 query functions work correctly with proper time filtering, grace period exclusion, and chronological ordering. Tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/macbookair/Documents/project/test/rerun7/GSD_Polymarket
source .venv/bin/activate
python -m pytest tests/test_evaluation_queries.py -v
python -c "from src.db.models import PerformanceSnapshot, TraderProfile; print('OK')"
python -m pytest tests/ -v --tb=short  # Ensure no regressions
```
All new tests pass. Existing tests still pass (no regressions from model additions).
</verification>

<success_criteria>
- PerformanceSnapshot stores all evaluation metrics per trader per timeframe
- TraderProfile stores profile classification with unique market count
- Time-windowed queries use composite indexes for efficiency
- Resolution grace period (4 hours) correctly excludes recently-resolved markets
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-historical-evaluation/03-04-SUMMARY.md`
</output>
