---
phase: 19-self-healing-token-catalog
task: review-fixes
total_tasks: review
status: in_progress
last_updated: 2026-02-27T03:44:20.659Z
---

<current_state>
Reviewing worker/19-01 branch. Reviewer found 3 bugs and applied 2 of 3 fixes.
Fix 3 is partially done — tests now fail because they still mock `gamma_client.client.get`
but the code was changed to use `httpx.get()` directly. Tests need `@patch("src.catalog.patcher.httpx.get")`
decorator added and `mock_httpx_get` parameter wired to each affected test.
</current_state>

<completed_work>

- Phase 19 planned: CONTEXT.md + RESEARCH.md + 19-01-PLAN.md + 19-02-PLAN.md written
- Worker executed both plans on branch worker/19-01 (commit defe357)
- 18/18 new tests passed before reviewer fixes
- Fix 1 DONE: Removed dead `_derive_niche_slug_from_tags()` function from patcher.py
- Fix 2 DONE: Replaced `gamma_client.client.get(...)` with `httpx.get(...)` + rate limiter acquire in `_try_tier2_api()`
- Fix 3 DONE (code): `_try_tier3_fallback()` now iterates ALL tokens per market (not just tokens_data[0])
- Fix 3 PARTIAL: Tests updated with sed to replace `mock_gamma_client.client.get.*` → `mock_httpx_get.*` but the `@patch` decorators and function signatures still need updating

</completed_work>

<remaining_work>

- Update test functions that use Tier 2 to add `@patch("src.catalog.patcher.httpx.get")` decorator and `mock_httpx_get` parameter
- Affected tests (grep confirmed these): test_tier1_null_tokens_falls_to_tier2, test_tier2_api_hit_with_esports_tags, test_tier2_api_hit_no_esports_tags, test_tier3_fallback_on_api_failure, test_both_token_ids_inserted_per_condition, test_niche_slug_from_category_sports, test_unknown_category_uses_api_tag, test_full_patch_flow_integration (8 tests)
- Re-run `python -m pytest tests/test_catalog_patcher.py tests/test_cli_catalog.py -q` → confirm 18/18 pass
- Commit reviewer fixes on worker/19-01
- Merge worker/19-01 → main
- Update REVIEW_QUEUE.md: move worker/19-01 from Pending → Cleared with reviewer notes
- Checkout main, verify merge clean

</remaining_work>

<decisions_made>

- Fix 2 approach: use `httpx.get()` at module level (matching gamma_client.py pattern) + `gamma_client.rate_limiter.acquire()` before call. `import httpx` moved inside the function (inline import) — acceptable, could be moved to top of file if preferred.
- Fix 3 approach: iterate all token entries in tokens_data list, same pattern as Tier 1 (`_try_tier1_local`)

</decisions_made>

<blockers>

- None — just mechanical test decorator updates remaining

</blockers>

<context>
The 3 bugs were caught by code inspection (not test failures initially):
1. Dead function `_derive_niche_slug_from_tags` — never called, just dead code
2. `gamma_client.client.get` — GammaMarketClient has no `.client` attr, uses `httpx.get()` directly at module level. Would have raised AttributeError at runtime.
3. Tier 3 only inserted 1 token per binary market — should insert both YES/NO tokens like Tier 1 does.

The overall patcher logic is solid. After test fixes, this is ready to merge.
</context>

<next_action>
Read tests/test_catalog_patcher.py lines ~95-420. For each of the 8 failing tests, add `@patch("src.catalog.patcher.httpx.get")` decorator above the test function and add `mock_httpx_get` as first parameter (after `self` if in a class, or first param if standalone). Then re-run tests.
</next_action>
