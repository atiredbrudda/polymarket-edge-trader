---
phase: 01-foundation
plan: 08
type: execute
wave: 2
depends_on: ["01-05", "01-06", "01-07"]
files_modified: [tests/test_integration.py]
autonomous: false

must_haves:
  truths:
    - "User confirms integration tests pass on corrected schema"
    - "User verifies schema changes don't break existing functionality"
    - "User approves proceeding to Phase 2 with corrected schema"
  artifacts:
    - path: ".planning/phases/01-foundation/01-08-SUMMARY.md"
      provides: "Gap closure verification report"
      min_lines: 50
  key_links:
    - from: "tests/test_integration.py"
      to: "src/polymarket_analytics/db/schema.py"
      via: "pytest execution"
      pattern: "test_schema_matches_guide"
---

<objective>
Human verification of gap closure and integration test results.

Purpose: Confirm schema corrections work correctly before proceeding to Phase 2.
Output: User approval to proceed, documented in summary.
</objective>

<execution_context>
@/Users/macbookair/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/macbookair/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-05-SUMMARY.md
@.planning/phases/01-foundation/01-06-SUMMARY.md
@.planning/phases/01-foundation/01-07-SUMMARY.md
@.planning/phases/01-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify gap closure and integration tests</name>
  <files>tests/test_integration.py, src/polymarket_analytics/db/schema.py</files>
  <action>
    Review schema gap closure work from plans 01-05, 01-06, 01-07:
    - trades: added trader_address, changed price/size to str (NUMERIC)
    - markets: added end_date, category, active, tokens
    - market_entities: replaced team with team_a/team_b, added tournament
    - positions: added outcome, trade_count, last_trade_timestamp
    - lift_scores: added category, position_count, total_pnl, computed_at, renamed fields
    - gamma_events: replaced JSON blob with normalized columns
    - Integration test: added test_schema_matches_guide
  </action>
  <verify>
    pytest tests/test_integration.py -v
    Expected: 4/4 tests pass (test_token_catalog_ingestion, test_zero_synthetic_market_ids, test_foreign_key_enforcement, test_schema_matches_guide)
  </verify>
  <done>User confirms all tests pass and schema matches GUIDE.md</done>
  <what-built>
    Schema gap closure (plans 01-05, 01-06, 01-07):
    - trades: added trader_address, changed price/size to str (NUMERIC)
    - markets: added end_date, category, active, tokens
    - market_entities: replaced team with team_a/team_b, added tournament
    - positions: added outcome, trade_count, last_trade_timestamp
    - lift_scores: added category, position_count, total_pnl, computed_at, renamed fields
    - gamma_events: replaced JSON blob with normalized columns
    - Integration test: added test_schema_matches_guide

    All changes align schema.py with GUIDE.md specification.
  </what-built>
  <how-to-verify>
    1. Run full integration test suite:
      ```bash
      pytest tests/test_integration.py -v
      ```

    2. Expected: 4/4 tests pass:
      - test_token_catalog_ingestion
      - test_zero_synthetic_market_ids
      - test_foreign_key_enforcement
      - test_schema_matches_guide

    3. Verify schema columns manually (optional):
      ```bash
      python -c "
      from src.polymarket_analytics.db.schema import init_database
      from pathlib import Path
      import tempfile

      with tempfile.NamedTemporaryFile(suffix='.db') as f:
          db = init_database(Path(f.name))

          print('trades columns:', [c.name for c in db['trades'].columns])
          print('markets columns:', [c.name for c in db['markets'].columns])
          print('market_entities columns:', [c.name for c in db['market_entities'].columns])
          print('positions columns:', [c.name for c in db['positions'].columns])
          print('lift_scores columns:', [c.name for c in db['lift_scores'].columns])
          print('gamma_events columns:', [c.name for c in db['gamma_events'].columns])
      "
      ```

    4. Confirm all expected columns present, no deprecated columns (team, data, etc.)
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass and schema is correct, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
- User has reviewed test results
- User confirms schema matches GUIDE.md
- User approves proceeding to Phase 2
</verification>

<success_criteria>
Gap closure complete and verified. All 8 schema divergences addressed:
- 1-4 (blockers): trader_address, end_date, gamma_events, team_a/team_b - FIXED
- 5-7 (important): lift_scores fields, positions fields, NUMERIC precision - FIXED
- 8 (moderate): sqlite-utils vs SQLAlchemy - ACCEPTABLE (documented deviation)
  </success_criteria>

  <output>
After completion, create `.planning/phases/01-foundation/01-08-SUMMARY.md` with user verification outcome
</output>
