---
phase: 01-foundation
plan: 10
type: execute
wave: 1
depends_on: []
files_modified: [src/polymarket_analytics/config/loader.py]
autonomous: true
gap_closure: true

must_haves:
truths:
  - "NicheConfig.tag_id stores integer values (not coerced strings)"
artifacts:
  - path: "src/polymarket_analytics/config/loader.py"
    provides: "NicheConfig with int type for tag_id"
    min_lines: 50
key_links:
  - from: "src/polymarket_analytics/config/loader.py"
    to: "YAML config files"
    via: "Pydantic validation"
    pattern: "tag_id: int"
---

<objective>
Fix tag_id type mismatch in NicheConfig - change from str to int to match YAML source data.

Purpose: YAML config files use integer tag IDs (64, 2, 745). Pydantic currently defines tag_id as str, causing silent coercion. This is semantically wrong and could break numeric comparisons.
Output: Corrected NicheConfig with tag_id: int type.
</objective>

<execution_context>
@/Users/macbookair/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/macbookair/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/polymarket_analytics/config/loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change NicheConfig.tag_id from str to int</name>
  <files>src/polymarket_analytics/config/loader.py</files>
  <action>
    Current definition in loader.py:
    ```python
    tag_id: str = Field(..., description="Polymarket tag ID for this niche")
    ```

    YAML files use integer tag IDs (e.g., 64, 2, 745). Pydantic coerces YAML integers to string, which is semantically wrong.

    Change to:
    ```python
    tag_id: int = Field(..., description="Polymarket tag ID for this niche")
    ```

    Also update the docstring comment in the class to reflect the type change:
    ```python
    """Configuration model for a Polymarket niche.

    Attributes:
        tag_id: Polymarket tag ID for this niche (required, integer)
        ...
    """
    ```

    And update the Field description if needed.

    After change, verify existing YAML configs still load correctly (Pydantic will parse integers as integers).
  </action>
  <verify>
    python -c "
from src.polymarket_analytics.config.loader import NicheConfig

# Test with integer value (as YAML provides)
config = NicheConfig(tag_id=64, slug='esports')
print(f'tag_id type: {type(config.tag_id)}')
print(f'tag_id value: {config.tag_id}')
assert isinstance(config.tag_id, int), f'Expected int, got {type(config.tag_id)}'
assert config.tag_id == 64, f'Expected 64, got {config.tag_id}'
print('tag_id is correctly typed as int')

# Verify numeric comparison works
assert config.tag_id > 0, 'Numeric comparison should work'
print('Numeric comparisons work correctly')
"
  </verify>
  <done>NicheConfig.tag_id is typed as int, stores integer values without coercion, numeric comparisons work</done>
</task>

</tasks>

<verification>
- tag_id type verified as int via isinstance() check
- Integer values load correctly from test data
- Numeric comparisons (>, <, ==) work on tag_id
</verification>

<success_criteria>
NicheConfig.tag_id correctly typed as int:
- YAML integer values (64, 2, 745) stored as integers (not coerced to strings)
- Numeric comparisons work correctly
- No breaking changes to config loading
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-10-SUMMARY.md`
</output>
