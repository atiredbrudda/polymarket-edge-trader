---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [src/polymarket_analytics/cli.py, src/polymarket_analytics/commands/__init__.py, src/polymarket_analytics/commands/build_token_catalog.py, src/polymarket_analytics/token_catalog/builder.py, src/polymarket_analytics/token_catalog/__init__.py]
autonomous: true

must_haves:
  truths:
    - "CLI entry point exists and accepts --niche flag"
    - "build-token-catalog command is callable with --niche flag"
    - "Token catalog builder module exists (stub implementation OK for Phase 1)"
artifacts:
  - path: "src/polymarket_analytics/cli.py"
    provides: "Click CLI group with --niche flag and config loading"
    exports: ["cli"]
  - path: "src/polymarket_analytics/commands/build_token_catalog.py"
    provides: "build-token-catalog CLI command"
    exports: ["build_token_catalog"]
  - path: "src/polymarket_analytics/token_catalog/builder.py"
    provides: "TokenCatalogBuilder class (stub for now)"
    exports: ["TokenCatalogBuilder"]
key_links:
  - from: "src/polymarket_analytics/cli.py"
    to: "niches/esports.yaml"
    via: "load_niche_config at CLI startup"
    pattern: "load_niche_config"
  - from: "src/polymarket_analytics/commands/build_token_catalog.py"
    to: "src/polymarket_analytics/token_catalog/builder.py"
    via: "import TokenCatalogBuilder"
    pattern: "from.*builder.*import.*TokenCatalogBuilder"
---

<objective>
Create Click CLI skeleton with --niche flag support and build-token-catalog command stub.

Purpose: CLI-01 and CLI-02 requirements mandate Click-based CLI with --niche flag on all commands - establish this pattern early so all future commands follow it.
Output: cli.py entry point, build-token-catalog command, TokenCatalogBuilder stub
</objective>

<execution_context>
@/Users/macbookair/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/macbookair/.config/opencode/get-shit-done/templates/summary.md
@GUIDE.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create Click CLI entry point with --niche flag</name>
  <files>src/polymarket_analytics/cli.py, src/polymarket_analytics/__init__.py</files>
  <action>
    Create src/polymarket_analytics/cli.py:

    1. @click.group() decorator with cli function
    2. @click.option("--niche", default="esports", help="Niche slug for config lookup")
    3. @click.pass_context decorator
    4. In cli function:
       - ctx.ensure_object(dict)
       - ctx.obj["niche"] = niche
       - Load niche config from niches/{niche}.yaml
       - Validate config exists before storing in context
       - ctx.obj["config"] = load_niche_config(config_path)

    5. Create src/polymarket_analytics/__init__.py:
       - Export cli for entry point
       - __version__ = "0.1.0"

    6. Add [project.scripts] to pyproject.toml:
       polymarket = "src.polymarket_analytics.cli:cli"

    Research confirms Click's context passing pattern (@click.pass_context) is required for commands to access shared state like config.
  </action>
  <verify>
    python -m src.polymarket_analytics.cli --help
    Shows: "Usage: python -m src.polymarket_analytics.cli [OPTIONS] COMMAND [ARGS]..."
    Shows: "--niche TEXT" option
    Shows: available commands list

    python -m src.polymarket_analytics.cli --niche esports --help
    Loads esports config without error
  </verify>
  <done>
    CLI entry point exists, --niche flag works, esports config loads on startup, help pages display correctly
  </done>
</task>

<task type="auto">
  <name>Create build-token-catalog command stub</name>
  <files>src/polymarket_analytics/commands/build_token_catalog.py, src/polymarket_analytics/commands/__init__.py</files>
  <action>
    Create src/polymarket_analytics/commands/build_token_catalog.py:

    1. @cli.command() decorator
    2. @click.option("--db-path", default="data/analytics.db", help="Path to SQLite database")
    3. @click.pass_context decorator
    4. Function build_token_catalog(ctx, db_path):
       - Access config: config = ctx.obj["config"]
       - Initialize database: db = init_database(Path(db_path))
       - Create TokenCatalogBuilder instance
       - Call builder.build(niche=config["slug"])
       - Print summary: "Built token catalog with N entries"

    5. For Phase 1 (stub): TokenCatalogBuilder.build() can return 0 entries or use fixture data
       - Real Gamma API integration comes in later phase
       - Stub should still demonstrate the wiring works

    Create src/polymarket_analytics/commands/__init__.py:
    - Import build_token_catalog (registers command with cli)

    Command should fail loudly if niche config missing (already handled by CLI startup).
  </action>
  <verify>
    python -m src.polymarket_analytics.cli --niche esports build-token-catalog --db-path /tmp/test.db
    Command executes without error (even if 0 entries)
    Output includes: "Building token catalog for niche: esports"
  </verify>
  <done>
    build-token-catalog command callable with --niche flag, accepts --db-path option, executes without error
  </done>
</task>

<task type="auto">
  <name>Create TokenCatalogBuilder stub module</name>
  <files>src/polymarket_analytics/token_catalog/builder.py, src/polymarket_analytics/token_catalog/__init__.py</files>
  <action>
    Create src/polymarket_analytics/token_catalog/builder.py:

    1. TokenCatalogBuilder class:
       - __init__(self, db: sqlite_utils.Database)
       - build(self, niche: str) -> int:
         - For Phase 1 stub: return 0 (or insert fixture data if available)
         - Log: "Token catalog builder initialized for niche: {niche}"
         - Future: Will fetch from Gamma API and insert into token_catalog table

    2. Why stub first: Research shows Gamma API endpoint structure unknown - start with fixture data, swap in real API later

    3. Design for extension:
       - Separate Gamma API client (gamma/client.py) will be created later
       - Builder will use client.fetch_token_catalog(niche) -> list[dict]
       - Then builder.upsert(tokens) using db.token_catalog.upsert_all()

    Create src/polymarket_analytics/token_catalog/__init__.py:
    - Export TokenCatalogBuilder
  </action>
  <verify>
    python -c "
    from src.polymarket_analytics.token_catalog.builder import TokenCatalogBuilder
    from src.polymarket_analytics.db.schema import init_database
    from pathlib import Path
    import tempfile
    builder = TokenCatalogBuilder(init_database(Path(tempfile.mktemp())))
    result = builder.build('esports')
    assert isinstance(result, int), f"Expected int, got {type(result)}"
    print(f"TokenCatalogBuilder instantiated, build() returned {result}")
    "
    TokenCatalogBuilder instantiates with database
    build() returns integer count
  </verify>
  <done>
    TokenCatalogBuilder class instantiates with database, build() method returns integer count
  </done>
</task>

</tasks>

<verification>
1. CLI --help shows --niche option and command list
2. CLI --niche esports loads config without error
3. build-token-catalog command registers with CLI
4. TokenCatalogBuilder class instantiates with database
5. build() method returns integer count
</verification>

<success_criteria>
- CLI-01: All commands use Click for CLI interface ✓
- CLI-02: All commands accept --niche flag for YAML config lookup ✓
- Token catalog builder module exists (stub for Phase 1, real implementation later) ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
