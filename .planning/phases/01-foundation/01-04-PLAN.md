---
phase: 01-foundation
plan: 04
type: tdd
wave: 2
depends_on: ["01-01", "01-02", "01-03"]
files_modified: [tests/test_integration.py, tests/conftest.py, tests/fixtures/gamma_responses/token_catalog_fixture.json,
src/polymarket_analytics/token_catalog/builder.py]
autonomous: true
must_haves:
  truths:
    - "Integration test runs with fixture data"
    - "Test asserts zero synthetic market_ids in trades table"
    - "Token catalog builder can insert fixture data"
  artifacts:
    - path: "tests/test_integration.py"
      provides: "TCAT-03 integration test for synthetic ID detection"
      min_lines: 40
    - path: "tests/conftest.py"
      provides: "Pytest fixtures for test database and config"
      exports: ["test_db", "niche_config"]
    - path: "tests/fixtures/gamma_responses/token_catalog_fixture.json"
      provides: "Fixture data for token catalog ingestion"
      contains: "token_id"
key_links:
  - from: "tests/test_integration.py"
    to: "src/polymarket_analytics/db/schema.py"
    via: "init_database for test setup"
    pattern: "init_database"
  - from: "tests/test_integration.py"
    to: "tests/fixtures/gamma_responses/token_catalog_fixture.json"
    via: "json.load for fixture data"
    pattern: "json\.\load.*fixture"
---

<objective>
Create integration test (TCAT-03) that asserts zero synthetic market_ids in trades table using fixture data.

Purpose: Critical test that token catalog prevents synthetic ID poisoning - must pass before any real trade ingestion.
Output: Integration test with fixture data, pytest fixtures, token catalog builder with fixture ingestion
</objective>

<execution_context>
@/Users/macbookair/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/macbookair/.config/opencode/get-shit-done/templates/summary.md
@GUIDE.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create pytest fixtures for test database and config</name>
  <files>tests/__init__.py, tests/conftest.py</files>
  <action>
    Create tests/__init__.py (empty file for pytest discovery).

    Create tests/conftest.py with fixtures:

    1. @pytest.fixture def test_db(tmp_path: Path) -> sqlite_utils.Database:
       - Creates temporary database path
       - Calls init_database(db_path)
       - Yields database for test use
       - Auto-cleanup when test completes (tmp_path handles this)

    2. @pytest.fixture def niche_config() -> NicheConfig:
       - Loads and returns validated esports config
       - Uses load_niche_config(Path("niches/esports.yaml"))

    3. @pytest.fixture def sample_token_catalog() -> list[dict]:
       - Returns list of sample token data for fixture ingestion
       - Each entry: {token_id, condition_id, question, niche_slug, node_path}
       - Include 5-10 sample eSports markets

    4. Create tests/fixtures/gamma_responses/ directory
       - Will hold token_catalog_fixture.json and future fixtures

    Research confirms pytest fixtures with tmp_path provide automatic cleanup and test isolation.
  </action>
  <verify>
    pytest tests/conftest.py --collect-only
    Shows: test_db, niche_config, sample_token_catalog fixtures available

    pytest --fixtures tests/
    Lists all fixtures with their scope and docstrings
  </verify>
  <done>
    Three fixtures defined: test_db (temp database), niche_config (validated esports config), sample_token_catalog (fixture data)
  </done>
</task>
<task type="auto">

  <name>Create fixture data for token catalog</name>
  <files>tests/fixtures/gamma_responses/token_catalog_fixture.json</files>
  <action>
    Create tests/fixtures/gamma_responses/token_catalog_fixture.json:

    ```json
    [
      {
        "token_id": "0x1a2b3c4d5e6f",
        "condition_id": "esports-iem-katowice-faze-vs-navi",
        "question": "Will FaZe win IEM Katowice 2025?",
        "niche_slug": "esports",
        "node_path": "esports/cs2/iem-katowice-2025/final"
      },
      {
        "token_id": "0x2b3c4d5e6f7a",
        "condition_id": "esports-lol-worlds-t1-vs-geng",
        "question": "Will T1 win Worlds 2025?",
        "niche_slug": "esports",
        "node_path": "esports/lol/worlds-2025/final"
      },
      {
        "token_id": "0x3c4d5e6f7a8b",
        "condition_id": "esports-dota2-ti-team-liquid",
        "question": "Will Team Liquid win TI 2025?",
        "niche_slug": "esports",
        "node_path": "esports/dota2/ti-2025/final"
      }
    ]
    ```

    This fixture provides:
    - Realistic token_id format (hex string)
    - condition_id matching Polymarket naming convention
    - Question text for display
    - niche_slug for filtering
    - node_path for hierarchy navigation

    Will be used by integration test and potentially by build-token-catalog stub.
  </action>
  <verify>
    python -c "
    import json
    with open('tests/fixtures/gamma_responses/token_catalog_fixture.json') as f:
      data = json.load(f)
    print(f'Loaded {len(data)} token entries')
    for token in data:
      assert 'token_id' in token
      assert 'condition_id' in token
      assert 'question' in token
      assert 'niche_slug' in token
      assert 'node_path' in token
    print('All required fields present')
    "
  </verify>
  <done>
    Fixture JSON with 3 token entries, each has token_id, condition_id, question, niche_slug, node_path
  </done>
</task>

<task type="auto">
  <name>Write integration test for TCAT-03 (zero synthetic IDs)</name>
  <files>tests/test_integration.py</files>
  <action>
    Create tests/test_integration.py:

    1. test_token_catalog_ingestion(test_db, sample_token_catalog):
       - Ingests sample tokens into token_catalog table
       - Asserts count matches expected (len(sample_token_catalog))
       - Asserts all token_ids queryable

    2. test_zero_synthetic_market_ids(test_db, sample_token_catalog):
       - TCAT-03: Critical integration test
       - Ingests sample tokens into token_catalog
       - Inserts sample trades referencing those token_ids
       - Runs SQL: SELECT COUNT(*) FROM trades LEFT JOIN token_catalog ON trades.token_id = token_catalog.token_id WHERE token_catalog.token_id IS NULL
       - Asserts count == 0 (zero synthetic IDs)

    3. test_foreign_key_enforcement(test_db):
       - Attempts to insert trade with non-existent token_id
       - Should raise IntegrityError (foreign key violation)
       - Confirms PRAGMA foreign_keys = ON is working

    Each test uses test_db fixture for isolated temporary database.
    Use db.token_catalog.insert_all() and db.trades.insert_all() for ingestion.
  </action>
  <verify>
    pytest tests/test_integration.py -v
    Shows: test_token_catalog_ingestion PASSED
    Shows: test_zero_synthetic_market_ids PASSED
    Shows: test_foreign_key_enforcement PASSED

    All 3 tests pass, no failures or errors
  </verify>
  <done>
    Integration test suite passes: token catalog ingestion works, zero synthetic IDs confirmed, foreign keys enforced
  </done>
</task>

<task type="auto">
  <name>Implement token catalog builder fixture ingestion</name>
  <files>src/polymarket_analytics/token_catalog/builder.py</files>
  <action>
    Update src/polymarket_analytics/token_catalog/builder.py:

    1. Add build_from_fixture(self, fixture_path: str) -> int:
       - Loads JSON fixture file
       - Validates each entry has required fields
       - Calls db.token_catalog.upsert_all(data, pk="token_id")
       - Returns count of entries inserted

    2. Update build(self, niche: str) -> int:
       - For Phase 1: calls build_from_fixture with hardcoded path
       - Logs: "Using fixture data for Phase 1 development"
       - Future: Will switch to Gamma API client

    3. Why upsert_all: sqlite-utils handles deduplication, type detection, batching

    This enables build-token-catalog command to actually populate token_catalog table with fixture data for manual testing.
  </action>
  <verify>
    python -m src.polymarket_analytics.cli --niche esports build-token-catalog --db-path /tmp/test-catalog.db
    Output: "Building token catalog for niche: esports"
    Output: "Using fixture data for Phase 1 development"
    Output: "Built token catalog with 3 entries"

    python -c "
    import sqlite_utils
    db = sqlite_utils.Database('/tmp/test-catalog.db')
    count = db['token_catalog'].count
    print(f'Token catalog has {count} entries')
    assert count == 3
    "
  </verify>
  <done>
    build-token-catalog command populates token_catalog table with 3 fixture entries
  </done>
</task>

</tasks>

<verification>
1. pytest fixtures defined and discoverable
2. Fixture JSON valid with all required fields
3. Integration test runs and passes
4. TCAT-03 assertion confirms zero synthetic IDs
5. Foreign key enforcement verified
6. build-token-catalog can populate catalog with fixture data
</verification>

<success_criteria>
- TCAT-03: Integration test asserts zero synthetic market_ids in trades table ✓
- Token catalog can be built from fixture data ✓
- Foreign keys enforced (attempts to insert orphan trades fail) ✓
- pytest suite runs and passes ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
