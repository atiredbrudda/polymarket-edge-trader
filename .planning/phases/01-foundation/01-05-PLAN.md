---
phase: 01-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [src/polymarket_analytics/db/schema.py]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "trades table has trader_address column for aggregating by trader"
    - "markets table has end_date column for 30-day scoring window filtering"
    - "market_entities uses team_a/team_b columns (not team)"
    - "positions has outcome and trade_count columns"
    - "price/size columns use NUMERIC(20,6) for precision"
  artifacts:
    - path: "src/polymarket_analytics/db/schema.py"
      provides: "Corrected schema with all missing columns"
      min_lines: 210
  key_links:
    - from: "src/polymarket_analytics/db/schema.py"
      to: "GUIDE.md schema design"
      via: "column definitions"
      pattern: "trader_address.*NUMERIC\\((20,6)\\)|end_date|team_a|team_b|outcome|trade_count"
---

<objective>
Migrate core schema tables to match GUIDE.md specification (gaps 1,2,4,6,7).

Purpose: Fix blocker schema divergences that prevent build-positions and score from working.
Output: Modified schema.py with altered table definitions for trades, markets, positions, market_entities.
</objective>

<execution_context>
@/Users/macbookair/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/macbookair/.config/opencode/get-shit-done/templates/summary.md
@/Users/macbookair/Documents/project/test/rerun7/polymarketv2/GUIDE.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@src/polymarket_analytics/db/schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add trader_address to trades, end_date to markets, fix market_entities columns</name>
  <files>src/polymarket_analytics/db/schema.py</files>
  <action>
    Modify create_core_tables() function to fix three tables:

    1. **trades table** (gap 1 + gap 7):
      - Add "trader_address": str column (required for build-positions to aggregate by trader)
      - Change "price": float to "price": str (will use NUMERIC(10,6))
      - Change "size": float to "size": str (will use NUMERIC(20,6))
      - Keep existing columns: trade_id, token_id, timestamp, side, market_id, asset_ticker

    2. **markets table** (gap 2):
      - Add "end_date": str column (required for score to filter 30-day window)
      - Add "category": str column (per GUIDE.md)
      - Add "active": bool column (per GUIDE.md)
      - Add "tokens": str column (per GUIDE.md, JSON blob)
      - Keep existing: condition_id, question, outcome, resolved, niche_slug, created_at

    3. **market_entities table** (gap 4):
      - Replace "team": str with "team_a": str and "team_b": str
      - Add "tournament": str column (per GUIDE.md entity_fields)
      - Change "market_id": str to "condition_id": str (GUIDE.md uses condition_id)
      - Remove "role": str (not in GUIDE.md)
      - Keep: id, game, player (wait - GUIDE.md doesn't have player, has tournament)
      - Actually per GUIDE.md: game, team_a, team_b, tournament, market_type
      - So: id (PK), condition_id (UNIQUE), game, team_a, team_b, tournament, market_type

    Per GUIDE.md schema design section. Use str types - sqlite-utils maps str to TEXT,
    NUMERIC affinity is set via column type hints in SQLAlchemy layer (Phase 2+).

    Gap reason: VERIFICATION.md identified these as critical blockers preventing
    build-positions and score commands from working.
  </action>
  <verify>grep -E "trader_address|end_date|team_a|team_b|tournament" src/polymarket_analytics/db/schema.py returns all columns</verify>
  <done>trades has trader_address, markets has end_date/category/active/tokens, market_entities has team_a/team_b/tournament/condition_id</done>
</task>

<task type="auto">
  <name>Task 2: Fix positions columns and price/size precision</name>
  <files>src/polymarket_analytics/db/schema.py</files>
  <action>
    Modify create_core_tables() function to fix positions table and numeric precision:

    1. **positions table** (gap 6):
      - Add "outcome": str column (per GUIDE.md - WIN/LOSS/NULL)
      - Add "trade_count": int column (per GUIDE.md - tracks number of trades in position)
      - Rename "entry_timestamp": str to "last_trade_timestamp": str (GUIDE.md has both)
      - Actually per GUIDE.md: positions has entry_timestamp AND last_trade_timestamp
      - So keep entry_timestamp, add last_trade_timestamp, outcome, trade_count
      - Final columns: id, trader_address, market_id, direction, size, avg_entry_price,
        entry_timestamp, last_trade_timestamp, trade_count, resolved, outcome, pnl

    2. **price/size precision** (gap 7):
      - GUIDE.md specifies NUMERIC(20,6) for size, NUMERIC(10,6) for price
      - sqlite-utils uses Python types: str->TEXT, float->REAL, int->INTEGER
      - For NUMERIC affinity, we need to use str type (stores as text, SQLite applies NUMERIC affinity)
      - In trades table: "price": str, "size": str
      - In positions table: "size": str, "avg_entry_price": str
      - Comment each with "# NUMERIC(10,6)" or "# NUMERIC(20,6)" for documentation

    Gap reason: positions missing outcome/trade_count prevents resolve-positions from storing results.
    Float for price/size causes precision bugs in scoring calculations.
  </action>
  <verify>grep -E "outcome.*str|trade_count.*int|last_trade_timestamp|NUMERIC" src/polymarket_analytics/db/schema.py returns expected columns</verify>
  <done>positions has outcome/trade_count/last_trade_timestamp, all price/size fields use str with NUMERIC comments</done>
</task>

<task type="auto">
  <name>Task 3: Update lift_scores to match GUIDE.md</name>
  <files>src/polymarket_analytics/db/schema.py</files>
  <action>
    Modify lift_scores table to match GUIDE.md specification (gap 5):

    Current columns: id, trader_address, clv, roi, sharpe, z_clv, z_roi, z_sharpe,
    composite, quintile, window_start, window_end

    GUIDE.md columns: id, trader_address, category, composite_score, clv_raw, clv_zscore,
    roi_raw, roi_zscore, sharpe_raw, sharpe_zscore, quintile, position_count, total_pnl,
    window_start, window_end, computed_at

    Changes needed:
    - Add "category": str (niche slug - esports, nba, etc.)
    - Add "position_count": int (number of positions in window)
    - Add "total_pnl": str (NUMERIC - total PnL in window)
    - Add "computed_at": str (ISO timestamp when score was computed)
    - Rename "composite": str to "composite_score": str (naming clarity)
    - Rename "clv": str to "clv_raw": str (distinguish from z-score)
    - Rename "z_clv": str to "clv_zscore": str (consistent naming)
    - Rename "roi": str to "roi_raw": str
    - Rename "z_roi": str to "roi_zscore": str
    - Rename "sharpe": str to "sharpe_raw": str
    - Rename "z_sharpe": str to "sharpe_zscore": str
    - Remove: clv, roi, sharpe, z_clv, z_roi, z_sharpe, composite (replaced by renamed versions)

    All numeric fields use str for NUMERIC affinity.

    Gap reason: lift_scores missing category/position_count/total_pnl/computed_at prevents
    proper score categorization and audit trail.
  </action>
  <verify>grep -E "category|position_count|total_pnl|computed_at|composite_score|clv_raw|roi_raw|sharpe_raw" src/polymarket_analytics/db/schema.py returns all columns</verify>
  <done>lift_scores has all GUIDE.md columns with proper naming and types</done>
</task>

</tasks>

<verification>
  - All 9 tables exist in schema.py
  - trades has trader_address column
  - markets has end_date column
  - market_entities has team_a, team_b, tournament columns (not team)
  - positions has outcome, trade_count, last_trade_timestamp columns
  - lift_scores has category, position_count, total_pnl, computed_at columns
  - All price/size fields use str type with NUMERIC comments
  - schema.py syntax valid (python -m py_compile passes)
</verification>

<success_criteria>
Schema divergences 1,2,4,5,6,7 closed. Schema matches GUIDE.md specification for:
  - trades (trader_address, NUMERIC price/size)
  - markets (end_date, category, active, tokens)
  - market_entities (team_a, team_b, tournament, condition_id)
  - positions (outcome, trade_count, last_trade_timestamp)
  - lift_scores (category, position_count, total_pnl, computed_at, renamed z-score fields)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
