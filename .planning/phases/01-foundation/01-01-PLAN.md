---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, src/polymarket_analytics/db/schema.py, src/polymarket_analytics/db/connection.py]
autonomous: true

must_haves:
  truths:
    - "Database has all 9 tables with correct types"
    - "SQLite WAL mode is enabled for read concurrency"
    - "Foreign keys are enforced on every connection"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project dependencies (sqlite-utils, click, pydantic, httpx, pyyaml, pytest)"
      contains: "sqlite-utils"
    - path: "src/polymarket_analytics/db/schema.py"
      provides: "All 9 table definitions with foreign keys and indexes"
      min_lines: 80
    - path: "src/polymarket_analytics/db/connection.py"
      provides: "Database connection factory with WAL mode and foreign key enforcement"
      exports: ["get_db"]
  key_links:
    - from: "src/polymarket_analytics/db/connection.py"
      to: "sqlite_utils.Database"
      via: "db.enable_wal() and PRAGMA foreign_keys = ON"
      pattern: "db\\.enable_wal\\(\\)"
    - from: "src/polymarket_analytics/db/schema.py"
      to: "src/polymarket_analytics/db/connection.py"
      via: "import get_db"
      pattern: "from.*connection.*import.*get_db"

---

<objective>
Create database schema with all 9 core tables, enable WAL mode, and establish connection factory with foreign key enforcement.

Purpose: Foundation for all data storage - token catalog, trades, positions, scores, and signals depend on correct schema.
Output: pyproject.toml with dependencies, schema.py with 9 tables, connection.py with WAL mode + FK enforcement
</objective>
<execution_context>
@/Users/macbookair/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/macbookair/.config/opencode/get-shit-done/templates/summary.md
@GUIDE.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Initialize Python project with dependencies</name>
  <files>pyproject.toml</files>
  <action>
    Create pyproject.toml using uv (preferred) or pip-compatible format:

    Project name: polymarket-analytics
    Dependencies: sqlite-utils, click, pydantic, httpx, pyyaml, pytest, aiolimiter

    Using uv:
    ```bash
    uv init polymarket-analytics
    uv add sqlite-utils click pydantic httpx pyyaml pytest aiolimiter
    ```

    Or create pyproject.toml manually with:
    - [project] section with name, version, requires-python
    - [project.dependencies] list with all libraries

    Create src layout: src/polymarket_analytics/__init__.py (empty file)
  </action>
  <verify>
    uv pip list OR pip list shows: sqlite-utils, click, pydantic, httpx, pyyaml, pytest, aiolimiter
    python -c "import sqlite_utils; import click; import pydantic; print('OK')" exits without error
  </verify>
  <done>
    All 7 dependencies installed and importable, src/polymarket_analytics package exists
  </done>
</task>

<task type="auto">
  <name>Create database connection factory with WAL mode</name>
  <files>src/polymarket_analytics/db/__init__.py, src/polymarket_analytics/db/connection.py</files>
  <action>
    Create src/polymarket_analytics/db/connection.py:

    1. get_db(db_path: Path) -> sqlite_utils.Database:
       - Creates sqlite_utils.Database instance
       - Calls db.enable_wal() once (SCHM-02 requirement)
       - Executes PRAGMA foreign_keys = ON for FK enforcement
       - Returns database connection

    2. Why sqlite-utils: Research confirms it's purpose-built for data pipelines (not ORM work)

    Create src/polymarket_analytics/db/__init__.py:
    - Export get_db from connection module

    WAL mode must be enabled at connection time, not per-query - it's a database-level setting that persists.
  </action>
  <verify>
    python -c "from src.polymarket_analytics.db.connection import get_db; from pathlib import Path; db = get_db(Path('/tmp/test.db')); print('WAL:', db.journal_mode); print('FK:', db.execute('PRAGMA foreign_keys').fetchone()[0])"
    Outputs: WAL: wal, FK: 1
  </verify>
  <done>
    get_db() returns sqlite_utils.Database with journal_mode='wal' and foreign_keys=1
  </done>
</task>

<task type="auto">
  <name>Define all 9 core tables with foreign keys and indexes</name>
  <files>src/polymarket_analytics/db/schema.py</files>
  <action>
    Create src/polymarket_analytics/db/schema.py with:

    1. init_database(db_path: Path) -> sqlite_utils.Database:
       - Calls get_db(db_path)
       - Creates tables in dependency order
       - Calls create_indexes(db)
       - Returns database

    2. create_core_tables(db) - All 9 tables:
       - traders: address (pk), first_seen, last_seen, backfill_complete (bool), created_at
       - markets: condition_id (pk), question, outcome (YES/NO/null), resolved (bool), niche_slug, created_at
       - market_entities: id (pk), hash, market_id, game, team, player, role (nullable), niche_slug
       - gamma_events: id (pk), event_type, market_condition_id, data (JSON), timestamp
       - token_catalog: token_id (pk), condition_id, question, niche_slug, node_path, created_at
       - trades: trade_id (pk), token_id (FK->token_catalog), timestamp, side (YES/NO), price, size, market_id
       - positions: id (pk, hash), trader_address (FK->traders), market_id, direction (LONG/SHORT/FLAT), size, avg_entry_price, entry_timestamp, last_trade_timestamp, pnl (nullable), resolved (bool)
       - lift_scores: id (pk, hash), trader_address (FK->traders), clv, roi, sharpe, z_clv, z_roi, z_sharpe, composite, quintile (1-5), window_start, window_end
       - signals: id (pk, hash), market_id, direction, q5_count, avg_score, first_seen, last_updated, alerted (bool)

    3. create_indexes(db) - Indexes for common queries:
       - token_catalog: condition_id, niche_slug
       - trades: token_id, market_id, timestamp
       - positions: trader_address, market_id, resolved
       - lift_scores: trader_address, quintile

    Use db.table().create() with pk, foreign_keys=[(col, ref_table, ref_col)], if_not_exists=True.
    Use hash_id for tables without natural string primary keys.
  </action>
  <verify>
    python -c "
    from src.polymarket_analytics.db.schema import init_database
    from pathlib import Path
    import tempfile
    with tempfile.TemporaryDirectory() as tmp:
        db = init_database(Path(tmp) / 'test.db')
        tables = [t.name for t in db.tables]
        print('Tables:', sorted(tables))
        assert len(tables) == 9, f'Expected 9 tables, got {len(tables)}'
    "
  </verify>
  <done>
    All 9 tables exist: traders, markets, market_entities, gamma_events, token_catalog, trades, positions, lift_scores, signals
  </done>
</task>

</tasks>

<verification>
1. All dependencies importable without errors
2. Database created with WAL mode enabled (journal_mode = 'wal')
3. Foreign key enforcement active (PRAGMA foreign_keys = 1)
4. All 9 tables exist with correct names
5. Foreign key constraints defined on trades->token_catalog, positions->traders, lift_scores->traders
</verification>

<success_criteria>
- SCHM-01: All 9 core tables created with correct types ✓
- SCHM-02: SQLite WAL mode enabled for read concurrency ✓
- Dependencies installed: sqlite-utils, click, pydantic, httpx, pyyaml, pytest, aiolimiter ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
