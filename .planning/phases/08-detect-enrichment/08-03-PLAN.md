---
phase: 08-detect-enrichment
plan: 03
type: tdd
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - tests/test_enrichment.py
autonomous: true
requirements:
  - ENRC-01
  - ENRC-02
  - ENRC-03
  - ENRC-04
  - ENRC-05
  - ENRC-06
  - ENRC-07
  - ENRC-08
  - ENRC-09

must_haves:
  truths:
    - "Tests assert all 4 new columns present on signals table after init_database()"
    - "Tests assert clv_dominant_count counts only traders with clv_zscore > 0"
    - "Tests assert tier is ACT/CONSIDER/WATCH based on q5_count"
    - "Tests assert avg_entry_price and min_entry_price reflect actual position entry prices"
    - "Tests assert avg_score is retained on every upserted signal"
    - "Tests assert enriched fields survive a full detect_convergence → upsert_signals_batch round-trip"
  artifacts:
    - path: "tests/test_enrichment.py"
      provides: "TDD integration tests for Phase 8 enrichment requirements"
      contains: "test_clv_dominant_count"
  key_links:
    - from: "tests/test_enrichment.py"
      to: "detection.convergence.detect_convergence"
      via: "calls detect_convergence and asserts new columns in result DataFrame"
      pattern: "detect_convergence"
    - from: "tests/test_enrichment.py"
      to: "detection.writer.upsert_signal"
      via: "calls upsert_signal with new params and reads back from signals table"
      pattern: "upsert_signal"
---

<objective>
Write TDD integration tests covering all 9 ENRC requirements. Tests run against an in-memory SQLite database using the same fixture helpers as test_detection.py.

Purpose: Prove all enrichment fields are correctly computed, persisted, and readable — providing regression protection for the full Phase 8 change set.
Output: tests/test_enrichment.py with ≥8 test functions, all passing.
</objective>

<execution_context>
@/Users/macbookair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/macbookair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-detect-enrichment/08-detect-enrichment-01-SUMMARY.md
@.planning/phases/08-detect-enrichment/08-detect-enrichment-02-SUMMARY.md
</context>

<interfaces>
<!-- Fixture helpers available from tests/conftest.py -->

```python
from tests.conftest import (
    create_q5_trader,      # creates trader + lift_scores with quintile=5
    create_qn_trader,      # creates trader + lift_scores with quintile N
    create_position,       # creates position record
    create_market,         # creates market record
    _FIXED_COMPUTED_AT,    # shared computed_at timestamp for MAX(computed_at) filter
)
```

create_q5_trader() creates lift_scores with clv_zscore=1.0 (positive) by default.
To create a Q5 trader with negative clv_zscore, insert lift_scores directly:
```python
db['lift_scores'].insert({
    'trader_address': addr, 'category': 'esports', 'composite_score': 1.0,
    'clv_raw': 0.1, 'clv_zscore': -0.5,   # negative!
    ...
    'quintile': 5, 'computed_at': _FIXED_COMPUTED_AT,
}, replace=True)
```

create_position() signature:
```python
def create_position(db, trader_address, market_id, direction, size,
                    avg_entry_price=0.5, resolved=False, outcome=None,
                    pnl=None, entry_timestamp=None, last_trade_timestamp=None) -> dict
```

create_market() end_date defaults to yesterday — markets will fail the
`datetime(m.end_date) > datetime('now')` filter. For open-market tests, pass a future end_date
by inserting into db['markets'] directly with end_date='2099-01-01T00:00:00Z'.

All fixtures must use _FIXED_COMPUTED_AT for computed_at so the MAX(computed_at) subquery
in detect_convergence() matches all test traders, not just the last inserted one.
</interfaces>

<feature>
  <name>Phase 8 enrichment tests</name>
  <files>tests/test_enrichment.py</files>
  <behavior>
Test 1 — schema migration columns present:
  init_database() → signals table columns include clv_dominant_count, avg_entry_price, min_entry_price, tier

Test 2 — clv_dominant_count counts only positive clv_zscore:
  2 Q5 traders; trader A has clv_zscore=1.5 (positive), trader B has clv_zscore=-0.3 (negative)
  Both positioned LONG on same open market
  detect_convergence result: clv_dominant_count == 1 (only trader A)

Test 3 — clv_dominant_count = 0 when all traders have negative clv_zscore:
  2 Q5 traders both with clv_zscore < 0, both LONG on same open market
  detect_convergence result: clv_dominant_count == 0

Test 4 — tier = CONSIDER for q5_count = 2:
  2 Q5 traders LONG on same open market
  detect_convergence result: tier == 'CONSIDER'

Test 5 — tier = ACT for q5_count >= 3:
  3 Q5 traders LONG on same open market
  detect_convergence result: tier == 'ACT'

Test 6 — avg_entry_price and min_entry_price reflect actual position prices:
  2 Q5 traders with avg_entry_price=0.30 and avg_entry_price=0.50 respectively
  detect_convergence result:
    avg_entry_price == 0.40 (average of 0.30 and 0.50)
    min_entry_price == 0.30 (minimum of the two)

Test 7 — upsert_signal persists all 4 new fields:
  call upsert_signal(..., clv_dominant_count=2, avg_entry_price=0.45, min_entry_price=0.40, tier='CONSIDER')
  query signals table: all 4 fields match expected values
  avg_score also present and correct (retention check per ENRC-09)

Test 8 — full round-trip: detect_convergence → upsert_signals_batch → read signals table:
  Setup same as Test 4 (2 Q5 traders, LONG, open market)
  Call detect_convergence, then upsert_signals_batch
  Read signals table: clv_dominant_count, avg_entry_price, min_entry_price, tier all populated
  avg_score present and non-null (ENRC-09 retention check)

Test 9 — upsert update preserves all enriched fields:
  Insert signal via upsert_signal with tier='CONSIDER'
  Update via upsert_signal with tier='ACT'
  Read signals table: tier == 'ACT', first_seen unchanged (existing behavior preserved)
  </behavior>
  <implementation>
Write tests/test_enrichment.py using pytest. Use the `detection_db` fixture pattern from
test_detection.py (tmp_path + init_database). Import fixture helpers from tests.conftest.

For open-market tests: insert directly into db['markets'] with end_date='2099-01-01T00:00:00Z'
so markets pass the convergence query's end_date filter. create_market() sets end_date to
yesterday which fails the filter — use direct insert for any test requiring convergence to fire.

For tests with non-default clv_zscore: insert into db['lift_scores'] directly with the desired
clv_zscore value and quintile=5, using _FIXED_COMPUTED_AT for computed_at.

Structure as a single TestEnrichment class with one method per test case listed in <behavior>.
Each test method is self-contained: setup → act → assert → no shared state between tests.

Run: `python -m pytest tests/test_enrichment.py -v` — all 9 tests must pass.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write TDD tests for Phase 8 enrichment (RED then GREEN)</name>
  <files>tests/test_enrichment.py</files>
  <behavior>
    - Test 1: signals table has all 4 new columns after init_database()
    - Test 2: clv_dominant_count=1 when one trader has clv_zscore>0 and one has clv_zscore<0
    - Test 3: clv_dominant_count=0 when all Q5 traders have clv_zscore<0
    - Test 4: tier='CONSIDER' when q5_count=2
    - Test 5: tier='ACT' when q5_count=3
    - Test 6: avg_entry_price=(p1+p2)/2 and min_entry_price=min(p1,p2)
    - Test 7: upsert_signal with new params → all 4 fields readable + avg_score retained
    - Test 8: full round-trip detect_convergence → upsert_signals_batch → signals table has all fields
    - Test 9: upsert update overwrites tier and new fields, preserves first_seen
  </behavior>
  <action>
RED phase: Write tests/test_enrichment.py with all 9 test methods. Run pytest — expect failures
if Plan 01/02 work is incomplete (but in normal execution order Plans 01+02 are done first).

GREEN phase: Confirm all 9 tests pass with `python -m pytest tests/test_enrichment.py -v`.

Key implementation details:
- Import: `from tests.conftest import create_q5_trader, create_qn_trader, create_position, create_market, _FIXED_COMPUTED_AT`
- Import: `from polymarket_analytics.detection.convergence import detect_convergence`
- Import: `from polymarket_analytics.detection.writer import upsert_signal, upsert_signals_batch`
- Import: `from polymarket_analytics.db.schema import init_database`
- For open markets: insert directly with end_date='2099-01-01T00:00:00Z'
- For custom clv_zscore: insert lift_scores directly (do not use create_q5_trader)
- Use _FIXED_COMPUTED_AT for all lift_scores.computed_at
  </action>
  <verify>
    <automated>cd /Users/macbookair/Documents/project/test/rerun7/polymarketv2 && python -m pytest tests/test_enrichment.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>9/9 tests in test_enrichment.py pass. Full test suite (pytest tests/) passes with no regressions.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/macbookair/Documents/project/test/rerun7/polymarketv2 && python -m pytest tests/ -v 2>&1 | tail -30
```

All existing tests plus new test_enrichment.py tests must pass. No skips or warnings about missing columns.
</verification>

<success_criteria>
1. tests/test_enrichment.py exists with 9 test methods in TestEnrichment class
2. All 9 tests pass (pytest tests/test_enrichment.py -v → 9 passed)
3. Full test suite passes (pytest tests/ → all passed)
4. Tests cover: schema columns (ENRC-01/02/03/04), clv_dominant_count logic (ENRC-05), avg/min entry price (ENRC-06/07), tier thresholds (ENRC-08), avg_score retention (ENRC-09)
</success_criteria>

<output>
After completion, create `.planning/phases/08-detect-enrichment/08-detect-enrichment-03-SUMMARY.md`
</output>
