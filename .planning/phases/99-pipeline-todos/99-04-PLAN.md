---
phase: 99-pipeline-todos
plan: 04
type: execute
wave: 1
depends_on: [pipeline-todo-03]
files_modified: [src/polymarket_analytics/db/schema.py, src/polymarket_analytics/commands/ingest_events.py, src/polymarket_analytics/commands/classify_tokens.py]
autonomous: true

must_haves:
  truths:
    - "gamma_events or markets table stores clobTokenIds from Gamma API"
    - "classify_tokens reads clobTokenIds from DB instead of re-fetching from API"
    - "No redundant 96K Gamma API calls during classify_tokens"
  artifacts:
    - path: "src/polymarket_analytics/db/schema.py"
      provides: "Migration to add clob_token_ids column to gamma_events or markets table"
      contains: "clob_token_ids"
    - path: "src/polymarket_analytics/commands/ingest_events.py"
      provides: "Stores clobTokenIds from Gamma API response"
      contains: "clob_token_ids"
    - path: "src/polymarket_analytics/commands/classify_tokens.py"
      provides: "Reads clobTokenIds from DB instead of fetching from API"
      no_api_call: "fetch_markets"
  key_links:
    - from: "src/polymarket_analytics/commands/classify_tokens.py"
      to: "src/polymarket_analytics/db/schema.py"
      via: "SELECT clob_token_ids FROM gamma_events or markets"
      pattern: "SELECT.*clob_token_ids"

---

<objective>
Eliminate redundant 96K Gamma API call from classify_tokens by storing clobTokenIds in the database during ingest-events, so classify-tokens reads from DB instead of making redundant API calls.

Purpose: classify_tokens currently fetches ALL markets from Gamma API again just to get clobTokenIds for token catalog - this is redundant since ingest-events already fetched these markets. Store clobTokenIds during ingest-events and read from DB in classify-tokens.
Output: Schema migration + ingest_events stores clobTokenIds + classify_tokens reads from DB (no API call)
</objective>
<execution_context>
@/Users/macbookair/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/macbookair/.config/opencode/get-shit-done/templates/summary.md
@GUIDE.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/polymarket_analytics/commands/ingest_events.py
@src/polymarket_analytics/commands/classify_tokens.py
@src/polymarket_analytics/db/schema.py
</context>

<tasks>

<task type="auto">
  <name>Add clob_token_ids column to markets table</name>
  <files>src/polymarket_analytics/db/schema.py</files>
  <action>
    Add migration in run_migrations() to add clob_token_ids TEXT column to markets table:
    
    1. Check if "clob_token_ids" column exists in markets table
    2. If not, add column via db["markets"].add_column("clob_token_ids", str)
    3. Store as JSON array string (e.g., '["token1", "token2"]')
    
    Why markets table (not gamma_events): token_catalog needs condition_id → clob_token_ids mapping, and markets table is the canonical source for market metadata.
  </action>
  <verify>
    Schema migration runs without error on existing database
    New databases have clob_token_ids column from start
  </verify>
  <done>
    Migration added, safe to run on existing or new databases
  </done>
</task>

<task type="auto">
  <name>Store clobTokenIds in ingest_events command</name>
  <files>src/polymarket_analytics/commands/ingest_events.py</files>
  <action>
    Modify _ingest_events_async() to extract and store clobTokenIds:
    
    1. Extract clob_token_ids from each market: market.get("clobTokenIds", [])
    2. Handle JSON string case: if isinstance(clob_token_ids, str), parse with json.loads()
    3. Convert to JSON string for storage: json.dumps(clob_token_ids)
    4. Add to markets_records dict: "clob_token_ids": json.dumps(clob_token_ids)
    5. Update INSERT/UPDATE SQL to include clob_token_ids column
    
    Keep the same upsert pattern (ON CONFLICT DO UPDATE).
  </action>
  <verify>
    ingest_events stores clob_token_ids in markets table for all ingested markets
    Re-runs update clob_token_ids correctly via ON CONFLICT DO UPDATE
  </verify>
  <done>
    markets.clob_token_ids populated with JSON array from Gamma API
  </done>
</task>

<task type="auto">
  <name>Modify classify_tokens to read from DB instead of API</name>
  <files>src/polymarket_analytics/commands/classify_tokens.py</files>
  <action>
    Replace Gamma API fetch with DB query:
    
    REMOVE:
    - GammaAPIClient import and instantiation
    - await client.fetch_markets() call
    - on_page callback for API progress
    
    ADD:
    - Query markets table: SELECT condition_id, question, outcome, category, clob_token_ids FROM markets WHERE niche_slug = ?
    - Parse clob_token_ids JSON: json.loads(row["clob_token_ids"]) if not None
    
    Flow:
    1. Query markets table for niche_slug
    2. Assert data exists (if empty, raise error telling user to run ingest-events first)
    3. Build token_catalog_records from DB rows instead of API response
    4. Keep existing logic for: outcome_list parsing, market_type detection, node_path building
    5. Keep fallback for NULL/empty clob_token_ids (synthetic IDs with warning)
    6. Keep filtering to known condition_ids (redundant now but harmless)
    
    Update CLI output messages to reflect DB read instead of API fetch.
  </action>
  <verify>
    classify_tokens builds token_catalog without calling Gamma API
    classify_tokens fails with clear error if markets table is empty (tells user to run ingest-events first)
    No httpx or GammaAPIClient calls in classify_tokens
  </verify>
  <done>
    classify_tokens reads from DB, zero Gamma API calls
  </done>
</task>

<task type="auto">
  <name>Add test for DB-first token catalog build</name>
  <files>tests/test_classify_tokens.py</files>
  <action>
    Create test file with 2-3 tests:
    
    1. test_classify_tokens_reads_from_db_not_api:
       - Insert test markets into markets table with clob_token_ids
       - Run classify_tokens
       - Assert token_catalog populated
       - Assert NO Gamma API calls made (use httpx mock or similar)
    
    2. test_classify_tokens_fails_without_markets:
       - Empty markets table
       - Run classify_tokens
       - Assert ClickException with helpful message about running ingest-events first
    
    3. test_classify_tokens_handles_null_clob_token_ids:
       - Insert market with NULL clob_token_ids
       - Run classify_tokens
       - Assert warning logged and synthetic IDs used (existing fallback behavior)
    
    Keep tests lightweight - this is a data flow fix, not algorithmic logic.
  </action>
  <verify>
    All new tests pass
    Existing tests still pass (no regressions)
  </verify>
  <done>
    Test coverage for DB-first behavior, error handling, and NULL fallback
  </done>
</task>

</tasks>

<verify_plan>
  <files>src/polymarket_analytics/db/schema.py, src/polymarket_analytics/commands/ingest_events.py, src/polymarket_analytics/commands/classify_tokens.py, tests/test_classify_tokens.py</files>
  <checks>
    - [ ] Schema migration adds clob_token_ids column safely
    - [ ] ingest_events stores clob_token_ids as JSON string
    - [ ] classify_tokens reads from DB, no Gamma API calls
    - [ ] classify_tokens fails gracefully if markets empty
    - [ ] Tests cover DB-first flow and error cases
    - [ ] All existing tests still pass
    - [ ] No debug artifacts (print statements, TODOs, hardcoded values)
  </checks>
</verify_plan>
